{
  "name": "external",
  "private": true,
  "description": "External interfaced server",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "polka": "^0.5.2",
    "request": "^2.88.0"
  }
}
